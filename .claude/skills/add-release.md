# Add release (Claude Code stub)

Load the full skill: `.cursor/skills/cookbook-add-release/SKILL.md`

Quick path:

1. Collect retro numbers + SHAs from vllm-omni `benchmark_results/`
2. Add `## vX.Y.Z` to each model `index.md` (newest first)
3. Update retro comparison table columns if needed
4. Add release block to top of `SUMMARY.md`
5. PR checklist in `.cursor/skills/vllm-omni-cookbook/references/templates.md`
